User Management API
A RESTful user management backend built with Node.js, Express, and MongoDB.
Implements JWT-based authentication and secure user operations.

🚀 Features
User Registration
User Login with JWT Authentication
Protected Routes
CRUD operations for users
Middleware-based authentication
🛠 Tech Stack
Node.js
Express.js
MongoDB
Mongoose
JSON Web Token (JWT)
📦 Installation
Clone the repository
Run npm install
Add your MongoDB connection string
Run npm start
📌 API Endpoints
POST /api/register
POST /api/login
GET /api/users
PUT /api/users/:id
DELETE /api/users/:id
